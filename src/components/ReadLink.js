import styled from '@emotion/styled';
import {Link} from 'gatsby';

const StyledReadLink = styled(Link)`
    display: inline-block;
    font-size: 0.7rem;
    text-decoration: none;
    font-weight: 100;
    cursor: pointer;
`;

export default StyledReadLink;